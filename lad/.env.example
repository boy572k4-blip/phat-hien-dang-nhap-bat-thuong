# Sao chép file này thành .env rồi đổi giá trị trước khi nộp đồ án.
# Không đưa file .env thật lên Git.

DB_PASSWORD=doi_mat_khau_nay
LAD_API_KEY=sinh_khoa_ngau_nhien_bang_lenh_ben_duoi

# Sinh khóa ngẫu nhiên:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

# Tùy chọn: hiệu chỉnh ngưỡng mà không phải sửa mã nguồn
# THRESHOLD_CHALLENGE=40
# THRESHOLD_BLOCK=70
# RULE_WEIGHT=0.6
# ML_WEIGHT=0.4
